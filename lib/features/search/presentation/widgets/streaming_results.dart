import 'package:flutter/material.dart';

class StreamingResults extends StatelessWidget {
  const StreamingResults({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(16.0),
      child: Center(
        child: Text('Results Stream Placeholder'),
      ),
    );
  }
}
